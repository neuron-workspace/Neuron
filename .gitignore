# Dependencies and generated application output
node_modules/
dist/
release/
coverage/
out/

# Local environment and credentials
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx

# Logs, caches, and OS/editor state
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/
.npm-cache/
.vite/
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent, model, and local automation state
.agents/
.codex/
.impeccable/

# Agent workflow and context state. Deliberately NOT tracked: this is tooling for
# building the app, not part of the app, and it does not belong in a public
# repository. The files stay on disk and are used locally.
#
# Consequence to know about: git worktrees are built from tracked content, so a
# delegated agent will not see these. Copy them into a new worktree by hand --
# see .claude/AGENT_PROTOCOL.md.
.claude/
AGENTS.md
CLAUDE.md
DECISIONS.md
HANDOFF.md
SHARED_TASKS.md
context-packs/
impeccable-reference/
skills-lock.json
tools/agent-*
tools/qa-*
tools/tmp-*

# Local QA captures and scratch content
*-preview.png
*-screenshot.png
notes/

# Playwright E2E output
test-results/
playwright-report/

# Code-signing private keys, never committed
*.pfx
*.p12

# Microsoft Store listing images (~5.7 MB). Generated, not drawn:
#   npm run store:logos
#   npm run build && npm run store:shots
# They are uploaded to Partner Center by hand and never read by the build, so
# the repository carried five megabytes of reproducible output. store-assets/
# README.md stays -- it maps each file to its Partner Center field, which the
# generators do not encode.
store-assets/logos/
store-assets/screenshots/
