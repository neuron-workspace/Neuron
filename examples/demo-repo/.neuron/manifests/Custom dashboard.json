{
  "name": "Custom dashboard",
  "version": "1.0.0",
  "description": "An HTML dashboard with its own CSS + JS; it reads workspace data and writes the projectStatus variable through the local API.",
  "permissions": [
    "notes.read",
    "tags.read",
    "variables.read",
    "variables.write"
  ],
  "allowedReadPaths": ["**"],
  "networkPolicy": "none"
}
